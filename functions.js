let emails = {
    "email1": "go1.com",
    "email2": "go2.com",
    'email3': 'go3.com'    
}

let scrimbaUsersEmails = Object.entries(emails);
let emailList = Object.values(emails);
let emailKeys = Object.keys(emails);

